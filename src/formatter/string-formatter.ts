module DateSlider.Formatter {
    export class StringFormatter extends AbstractFormatter {
        /**
         * Formats a string from a DateSliderModel object.
         */
        public formatInput(input: DateSliderModel, format?: string, culture?: string): string | DateSliderInvalidOutput {
            return null;
        }
    }
}
