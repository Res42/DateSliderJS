module DateSlider.Formatter {
    // TODO: file: 'unix-timestamp-ms-formatter.ts'
    // severity: 'Error'
    // message: 'A class must be declared after its base class.'
    // at: '2,51'
    // source: 'ts'
    export class UnixTimestampMsFormatter extends AbstractFormatter {
        /**
         * Formats a unix timestamp (in seconds) from a DateSliderModel object.
         */
        public formatInput(input: DateSliderModel): number | DateSliderInvalidOutput {
            return null;
        }
    }
}
